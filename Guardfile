guard :compass do
  watch(%r{sass\.scss$})
end

guard 'livereload', host: '127.0.0.1', port: '35729' do
  watch(%r{css/.+\.css$})
  watch(%r{js/.+\.js$})
  watch(%r{templates/.+\.php$})
  watch(%r{template\.php$})
end
